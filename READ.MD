git log -----> gives you all the user in the project
git config user.name ------> tells you the username of the git system
git config --global user.name "chinedu_umeh". ----> sets or change the username to the name in strings, from this example, the user name is chinedu_umeh

git repo - repository - is just a git work space---[***just folder]
